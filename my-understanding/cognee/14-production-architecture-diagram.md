# 14 - Production Architecture Diagram

This diagram shows a production deployment where multiple existing agents use Cognee as a shared memory service.

## PlantUML Source

- `my-understanding/diagrams/cognee-production-architecture.puml`

## Render Example

If PlantUML is installed locally:

```bash
plantuml my-understanding/diagrams/cognee-production-architecture.puml
```

This generates an image (for example PNG) next to the `.puml` file.
